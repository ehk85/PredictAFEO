# Dependances supplementaires pour reentrainer le modele et utiliser le notebook
# (non necessaires pour faire tourner l'application deployee).
-r requirements.txt
lxml>=4.9
openpyxl>=3.1
jupyter>=1.0
